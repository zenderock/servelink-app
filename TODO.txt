- [ ] Clean up log-list.html (too many params)
- [ ] Add cancel and skip
- Mobile design
- [ ] Review logs page
- Check Redis messages in project vs deploymentservice
- Fix selected in Basecoat (if no items). Check for dropdown too
- Clean up end/start logic for logs (both deployment and /logs): probably need to have fixed intervals (e.g. 30 days/7 days etc) and custom dates WITHIN that interval. We may also need to save build logs then for archival...?
- Fix responsive
- LOGS:
    - Fix height
    - Fix loading animation
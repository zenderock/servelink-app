{
  "scripts": {
    "build": "npm run css:build && npm run email",
    "css:dev": "tailwindcss --silent -i ./src/main.css -o ./app/static/styles.css --watch",
    "css:build": "tailwindcss -i ./src/main.css -o ./app/static/styles.css --minify",
    "email": "mjml src/login.mjml -o app/templates/email/login.html"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.9",
    "mjml": "^4.15.3",
    "tailwindcss": "^3.4.15"
  }
}

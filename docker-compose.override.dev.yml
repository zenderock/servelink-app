services:
  # app:
  #   build:
  #     dockerfile: Docker/Dockerfile.app.dev
  #   volumes:
  #     - ./web:/app
  #     - ./shared:/shared
  #     - ./data/db:/data/db
  #     - ./data/traefik:/data/traefik
  #     - ./data/upload:/app/app/static/upload
  
  api:
    build:
      dockerfile: Docker/Dockerfile.api.dev
    volumes:
      - ./api:/app
  
  traefik:
    volumes:
      - ./data/traefik:/etc/traefik/dynamic_conf